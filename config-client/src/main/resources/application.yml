spring:
  application:
    name: config-client
  cloud:
    discovery:
      enabled: true
      service-id: config-server
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
server:
  port: 8881
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/


